{% extends 'base.html.twig' %}

{% block menu_principal %}
    {{ include('Menu/menuprincipal.html.twig') }}
{% endblock %}

{% block title %}
    Movilnet::Roles Listar
{% endblock %}

{% block body %}
    <div class="box">
        <div class="box-header with-border">
            <i class="fa fa-users fa-fw"></i>
            Roles
            <div class="box-tools">
                <a class="btn btn-default btn-sm" href="{{ path('settings_index') }}">
                    <i class="fa fa-arrow-left"></i>&nbsp;
                    Configuraciones
                </a>
                {% if is_granted('ROLE_GROUP_ADD') %}
                    <a class="btn btn-default btn-sm" href="{{ path('group_add') }}">
                        <i class="fa fa-plus"></i>&nbsp;
                        Agregar Rol
                    </a>
                {% endif %}
            </div>
        </div>
    </div>
    <div class="box">
        <div class="box-body">
            <table class="table" id="tableGrupos">
                <thead>
                <tr>
                    <th>Nombre</th>
                    <th class="text-right"><i class="fa fa-cogs fa-fw"></i>Acciones</th>
                </tr>
                </thead>
                {% for grupo in grupos %}
                    <tr>
                        <td>{{ grupo.name }}</td>
                        <td class="text-right">
                            {% if is_granted('ROLE_GROUP_DETAILS') %}
                                <a class="btn btn-default btn-sm"
                                   href="{{ path('group_detail', { 'id' : grupo.id }) }}">
                                    <i class="fa fa-cog fa-fw"></i>Detalle
                                </a>
                            {% endif %}
                        </td>
                    </tr>
                {% endfor %}
            </table>
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    <script>
        $(function () {
            $('#tableGrupos').DataTable({
                "oLanguage": {
                    "sProcessing": '<div class="text-center" style="font-size: 16px">Cargando, favor espere ... </div>' + '<i class="fa fa-spinner fa-spin fa-3x fa-fw"></i><span class="sr-only">Cargando...</span> ',
                    "sInfo": "Un total de _TOTAL_ registros para mostrar (_START_ a _END_)",
                    "sLengthMenu": "Mostrando _MENU_ registros",
                    "oPaginate": {
                        "sNext": "Siguiente",
                        "sPrevious": "Anterior"
                    },
                    "sEmptyTable": "No se encontraron registros",
                    "sInfoEmpty": "_TOTAL_ registros",
                    "sSearch": "Buscar"
                }
            });
        });
    </script>
{% endblock %}